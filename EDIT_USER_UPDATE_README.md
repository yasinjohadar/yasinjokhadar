# تحديث صفحة تعديل المستخدم

## الميزات المضافة

### 1. تصميم موحد مع صفحة الإنشاء
- **نفس التخطيط**: استخدام نفس التصميم والأنماط
- **نفس الحقول**: جميع الحقول الموجودة في صفحة الإنشاء
- **نفس التحقق**: نفس قواعد التحقق من صحة البيانات

### 2. الحقول المحدثة

#### المعلومات الأساسية
- ✅ **الاسم الكامل** - مطلوب
- ✅ **اسم المستخدم** - اختياري
- ✅ **البريد الإلكتروني** - مطلوب
- ✅ **رقم الهاتف** - اختياري

#### كلمة المرور (اختيارية في التعديل)
- ✅ **كلمة المرور الجديدة** - اختياري
- ✅ **تأكيد كلمة المرور** - اختياري
- 📝 **ملاحظة**: إذا تركت فارغة، لن يتم تغيير كلمة المرور

#### صورة المستخدم
- ✅ **عرض الصورة الحالية** - تظهر الصورة الموجودة
- ✅ **رفع صورة جديدة** - مع معاينة فورية
- ✅ **حذف الصورة القديمة** - تلقائياً عند رفع صورة جديدة

#### حالة المستخدم
- ✅ **حالة المستخدم** - نشط/غير نشط/محظور
- ✅ **تفعيل الحساب** - سويتش لتفعيل/إلغاء تفعيل

#### الأدوار
- ✅ **اختيار متعدد للأدوار** - مع Select2
- ✅ **عرض الأدوار الحالية** - محددة مسبقاً

### 3. التحسينات التقنية

#### التحقق من صحة البيانات
```php
// التحقق من التفرد مع استثناء المستخدم الحالي
'email' => 'required|string|email|max:255|unique:users,email,' . $id,
'username' => 'nullable|string|max:255|unique:users,username,' . $id,
'phone' => 'nullable|string|max:20|unique:users,phone,' . $id,
```

#### معالجة الصور
```php
// حذف الصورة القديمة
if ($user->photo) {
    \Storage::disk('public')->delete($user->photo);
}

// حفظ الصورة الجديدة
$photoPath = $photo->storeAs('users/photos', $photoName, 'public');
```

#### كلمة المرور الاختيارية
```php
// تحديث كلمة المرور فقط إذا تم إدخالها
if ($request->filled('password')) {
    $updateData['password'] = Hash::make($request->password);
}
```

### 4. الملفات المحدثة

#### الواجهة
- `resources/views/admin/pages/users/edit.blade.php`
  - تصميم جديد موحد مع صفحة الإنشاء
  - جميع الحقول المطلوبة
  - معاينة الصور
  - Select2 للأدوار

#### التحكم
- `app/Http/Controllers/Admin/UserController.php`
  - دالة `update()` محدثة
  - تحقق شامل من صحة البيانات
  - معالجة الصور
  - تحديث الأدوار

### 5. الميزات الجديدة

#### معاينة الصور
```javascript
function previewPhoto(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function(e) {
            document.getElementById('photo-preview').src = e.target.result;
        };
        reader.readAsDataURL(input.files[0]);
    }
}
```

#### Select2 للأدوار
```javascript
$('select[name="roles[]"]').select2({
    placeholder: "اختر الأدوار",
    allowClear: true,
    dir: "rtl"
});
```

### 6. كيفية الاستخدام

#### تعديل معلومات أساسية
1. انتقل إلى صفحة تعديل المستخدم
2. عدل الاسم أو البريد الإلكتروني أو الهاتف
3. احفظ التغييرات

#### تغيير كلمة المرور
1. أدخل كلمة المرور الجديدة
2. أكد كلمة المرور
3. احفظ التغييرات
4. **ملاحظة**: إذا تركت فارغة، لن تتغير كلمة المرور

#### تغيير الصورة
1. انقر على "اختر صورة"
2. اختر الصورة الجديدة
3. ستظهر معاينة فورية
4. احفظ التغييرات

#### تغيير الأدوار
1. استخدم القائمة المنسدلة للأدوار
2. اختر الأدوار المطلوبة (يمكن اختيار أكثر من دور)
3. احفظ التغييرات

### 7. الأمان

#### التحقق من التفرد
- البريد الإلكتروني فريد (مع استثناء المستخدم الحالي)
- اسم المستخدم فريد (مع استثناء المستخدم الحالي)
- رقم الهاتف فريد (مع استثناء المستخدم الحالي)

#### معالجة الملفات
- التحقق من نوع الملف (صور فقط)
- التحقق من حجم الملف (أقل من 2 ميجابايت)
- حذف الملفات القديمة تلقائياً

#### التحقق من صحة البيانات
- التحقق من صحة البريد الإلكتروني
- التحقق من طول كلمة المرور
- التحقق من تأكيد كلمة المرور

### 8. التصميم

#### الأنماط المضافة
```css
.form-floating label {
    right: auto;
    left: 0.75rem;
}

.photo-preview {
    width: 100px;
    height: 100px;
    border-radius: 50%;
    object-fit: cover;
    border: 2px solid #e9ecef;
}
```

#### التخطيط
- استخدام Bootstrap Grid System
- تخطيط متجاوب
- تصميم موحد مع باقي النظام

## ملاحظات مهمة

1. **كلمة المرور اختيارية** في التعديل
2. **الصورة القديمة تُحذف** عند رفع صورة جديدة
3. **الأدوار تُحدث** تلقائياً عند الحفظ
4. **جميع الحقول محمية** من الأخطاء
5. **التصميم متجاوب** مع جميع الأجهزة 
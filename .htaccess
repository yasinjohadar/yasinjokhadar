<IfModule mod_rewrite.c>
    RewriteEngine On

    # إعادة توجيه كل الطلبات إلى مجلد public (الراوت = public)
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
